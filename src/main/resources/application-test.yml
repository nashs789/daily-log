server:
  port: 8081

spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: ["openid", "profile", "email"]
            redirect-uri: "http://localhost:8081/login/oauth2/code/google"
  datasource:
    url: jdbc:postgresql://localhost:5432/life_log
    username: test
    password: test
    hikari:
      maximum-pool-size: 10
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate.format_sql: true
      hibernate.jdbc.time_zone: Asia/Seoul
  flyway:
    enabled: true
    locations: classpath:db/migration

notify:
  enabled: true
  slack:
    enabled: true
    webhook: ${SLACK_WEBHOOK_URL}
  discord:
    enabled: true
    webhook: ${DISCORD_WEBHOOK_URL}

url:
  script:
    marked: https://cdn.jsdelivr.net/npm/marked@12.0.2/marked.min.js
    safe-guard: https://cdn.jsdelivr.net/npm/dompurify@3.1.6/dist/purify.min.js
    jquery: /webjars/jquery/3.7.1/jquery.min.js
  base: https://inbok.shop
  jsp: /WEB-INF/views
  css: /css
  image: /image

auth:
  cookie:
    domain: inbok.shop
    secure: true # 로컬 http면 false
    same-site: Lax
    access-name: ACCESS
    refresh-name: REFRESH
    access-max-age-mins: 15
    refresh-max-age-days: 14

jwt:
  issuer: lifelog
  secret: ${JWT_SECRET}
  access-exp-minutes: 15
  refresh-exp-days: 14