# 📌Daily Log

디스코드 운동 일지 작성을 위한 템플릿 이용 및 기록 관리 시스템
- 개발기간: 2025.10.22 ~ 2025.11.16
- 프로젝트 기획 이유: 싱장에서 함께 운동하는 팀원들이 매일 디스코드에 운동 일지를 작성 하고있는데 반복적인 작업이 번거롭다고 생각되어 템플릿 자동화 및 일지 관리 시스템을 구축하게 됨
  - 템플릿을 매번 복사/붙여넣기 해야함
  - 사람마다 템플릿 형식이 일정하지 않음
  - 운동에 따라서 다른 템플릿을 사용함

------------
# 📌프로젝트 개요
Google OAuth2 기반 로그인 → JWT 발급 → 템플릿 입력 → Discord Webhook 자동 전송까지 운동 기록을 자동화하는 서비스입니다.

- Jenkins + Home Server 기반 CI/CD
- Docker 테스트 환경 + TDD 기반 개발
- Swagger 문서화
- DataTables 기반 관리자 UI
- PostgreSQL 기반 영속

------------
# 🏗 시스템 아키텍처

                            ┌───────────────────────────┐
                            │        User Browser       │
                            │  (JSP, jQuery, DataTables)│
                            └─────────────┬─────────────┘
                                          │ HTTP
                                          ▼
                             ┌───────────────────────────┐
                             │      Spring MVC API       │
                             │ Java · JSP · JPA · Swagger│
                             └─────────────┬─────────────┘
                                           │
           ┌───────────────────────────────┼──────────────────────────────┐
           │                               │                              │
           ▼                               ▼                              ▼
    ┌─────────────┐              ┌────────────────┐              ┌─────────────────┐
    │ Google OAuth│              │ JWT Provider   │              │ Discord Webhook │
    │  (OAuth2)   │              │ Access Tokens  │              │  Message Send   │
    └─────────────┘              └────────────────┘              └─────────────────┘
                                         │
                                         │
                                         ▼
                                ┌──────────────────┐
                                │   JPA Repository │
                                │   PostgreSQL DB  │
                                └───────┬──────────┘
                                        │
                                        │ via Docker(Test) / Local(Prod)
                                        ▼
                          ┌───────────────────────────────┐
                          │   CI/CD Pipeline (Jenkins)    │
                          │ Jenkinsfile → Test → Deploy   │
                          │   (Home Server Deployment)    │
                          └───────────────────────────────┘
------------
# 🧪 테스트 환경(TDD + Docker)
                ┌─────────────┐
                │  JUnit5     │
                │  Mockito    │
                └──────┬──────┘
                       │
                       ▼
       ┌────────────────────────────────┐
       │ Testcontainers / Docker Compose│
       │   - PostgreSQL Test DB         │
       │   - 데이터 시드 자동 생성           │
       └────────────────────────────────┘
                         │
                         ▼
       모든 테스트 통과 시에만 → Jenkins Build → Deploy

------------
# ⚙️ 기술 스택

- Backend
  - Java 17
  - Spring MVC
  - JPA(Hibernate)
  - PostgreSQL
  - Swagger
  - JUnit5, Mockito
  - Docker(Test DB)

- Frontend
  - JSP
  - jQuery
  - DataTables
  - marked.js
  - 
- DevOps / Infra
  - Jenkins CI/CD
  - Home Server Deployment
  - GitHub push → Jenkins → Build → Deploy
  - Docker(Test Environment)

------------
# 🚀 배포 파이프라인

    GitHub Push
    │
    ▼
    Jenkins Webhook Trigger
    │
    ├── Docker 기반 테스트(PostgreSQL)
    ├── JUnit5 + Mockito 테스트 실행
    │
    └── 모든 테스트 통과 시에만 배포
    │
    ▼
    Home Server (Spring Boot JAR or WAS)
